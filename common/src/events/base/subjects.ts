export enum Subjects {
  AssetCreated = "asset.created",
  AssetUpdated = "asset.updated",
  OrderUpdated = "order.updated",
}

export enum Streams {
  Asset = "ASSET",
  Order = "ORDER",
}
